# sidoc

Monorepo untuk Sidoc — aplikasi pengelolaan dokumen.

## Struktur
- `frontend/` — Next.js (UI)
- `backend/` — Express (API)

## Branch strategy
- `main` — release/stable
- `dev` — development integration

## Setup singkat (development)
1. Clone repo  
```bash
git clone git@github.com:username/sidoc.git
cd sidoc
